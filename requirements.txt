# VPMA Dependencies — REDIRECT
#
# The actively maintained requirements file is backend/requirements.txt.
# This root file exists for reference only. Install from:
#
#   cd backend && pip install -r requirements.txt
#
# See backend/requirements.txt for current versions and all dependencies.
